---
description: Unit-test conventions
globs:
  - tests/**/*.py
alwaysApply: false
---

* Prefer `pytest` fixtures over setup/teardown methods.
* Name files `test_*.py`; keep tests < 100 lines when possible.
* Use parametrization for matrix cases rather than loops.
